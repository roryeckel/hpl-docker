version: "3"

networks:
    default:
        external: false

services:
    hpl:
        image: hpl
        networks:
            - default
        ports:
            - "2222:22"
        environment:
            - AUTHORIZED_KEYS=
            - KNOWN_HOSTS=